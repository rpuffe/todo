{
  "name": "todo-api",
  "version": "1.0.0",
  "private": true,
  "description": "In-memory JSON REST API for managing todo items (flightdeck app).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
